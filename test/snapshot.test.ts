// Deprecated legacy snapshot test file intentionally minimized. Use tests/css-snapshot.spec.ts instead.
import { describe, it } from 'vitest';

describe('deprecated snapshot.test', () => {
  it('noop', () => {});
});
