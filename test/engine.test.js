// Deprecated legacy engine test file intentionally minimized. Use tests/engine.spec.ts instead.
import { describe, it } from 'vitest';
describe('deprecated engine.test', () => {
    it('noop', () => { });
});
