# Prompt: Test and Merge Task 005

**Task:** [TASK-005](../tasks/TASK-005-CODEX-dcv-oklch-contrast-bug.md)
**Status:** in-progress (code integrated into main; final closeout owned by Codex)
**Branch:** `task/005-oklch-contrast-bug` (Codex)
**Merged:** 2026-06-12 → `main` (merge commit `d21695d`, `--no-ff`)

## Source

- agent: Codex (fix) + Claude (integration)
- model: GPT-5.3-Codex / claude-opus-4-8

---

## Context

Codex committed the OKLCH fix as `d67bb1b fix(TASK-005): correct OKLCH contrast
conversion` on `task/005-oklch-contrast-bug`. That branch was cut **before**
TASK-007 de-committed build output, so the commit also carried compiled
artifacts. The merge below brings the real source and strips the artifacts.

## Merge to Main

```bash
git checkout main
git merge --no-ff task/005-oklch-contrast-bug   # modify/delete conflict on core/color.js
git rm -f core/color.js test/color.test.{js,d.ts,d.ts.map}   # drop reintroduced artifacts
git commit                                       # → merge commit d21695d
```

- [x] `core/color.ts` (the fix) merged
- [x] `test/color.test.ts` (Codex's boundary test) merged
- [x] Reintroduced artifacts dropped (`core/color.js`,
      `test/color.test.{js,d.ts,d.ts.map}`) — stay gitignored, regenerated by build

## Post-Merge Verification (on main)

- [x] `npm run build` → exit 0
- [x] `npm test` → **84 passed, 2 skipped** (Codex's `color.test.ts` adds 10
      passing tests; the OKLCH fix is exercised)
- [x] `git ls-files | grep -E '\.(js|d\.ts|map)$'` → zero source-dir artifacts
- [x] `npm run workflow:test` → green

## Open / Owned by Codex

- [ ] Status → done + `DONE-TASK-005` rename, once Codex confirms all TASK-005
      acceptance criteria are met (the engine-math review and any DecisionThemes
      follow-up are Codex's call, not integrated here).
- [ ] The `task/005-oklch-contrast-bug` branch was **merged, not rewritten** —
      Codex's `d67bb1b` is preserved in history. The branch can be deleted once
      Codex no longer needs its worktree.
